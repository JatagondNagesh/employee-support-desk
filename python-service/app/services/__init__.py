"""Business logic: text extraction, policy retrieval and answer generation, document review.

These modules depend on models, policies and model_provider; nothing here imports main, so the
dependency direction stays one way: main -> services -> domain.
"""
